{
  "name": "scale-pulse",
  "private": true,
  "version": "0.0.0",
  "description": "Scale Pulse — webapp + iOS (SwiftUI) + shared core",
  "scripts": {
    "dev": "npm run dev --prefix webapp",
    "build": "npm run build --prefix webapp",
    "start": "npm run start --prefix webapp",
    "dev:web": "npm run dev --prefix webapp",
    "build:web": "npm run build --prefix webapp",
    "start:web": "npm run start --prefix webapp",
    "build:pages": "npm run build:pages --prefix webapp",
    "dev:pages": "npm run dev:pages --prefix webapp",
    "test": "npm run test --prefix packages/core"
  }
}
