/** Re-export shared fretboard from @scale-pulse/core. */
export {
  STANDARD_TUNING_MIDI,
  FRET_MIN,
  FRET_MAX,
  POSITIONS,
  getPosition,
  inPosition,
  noteMidiAt,
  scaleDots,
  type StringIndex,
  type PositionId,
  type FretPosition,
  type FretDot,
} from "@scale-pulse/core";
