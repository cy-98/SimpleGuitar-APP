# Scale Pulse

手机优先的 **节拍器 + 大调级数练习** 工具。一屏用完，无整页滚动。底部三个 Tab：**节拍 / 级数 / 设置**。

---

## 1. 一句话定位

练节奏用节拍器；练调内感用「环图 / 吉他指板」看级数、听唱名。两者独立，设置里改声源、音量、主题。

---

## 2. 信息架构

```
Scale Pulse
├── 节拍（默认页）
│   ├── 中央：拍位可视化 + Play / Pause
│   └── 控件：速度 / 拍号 / 细分
├── 级数
│   ├── 当前大调 + 唱名开关
│   ├── 调芯片（选调）
│   ├── 主视觉：环图 或 指板
│   ├── 把位（仅指板）
│   └── 底部：环图 | 指板 切换
└── 设置
    ├── 声源
    ├── 音量
    ├── 反拍静音
    └── 配色
```

---

## 3. 节拍页

### 3.1 中央舞台

| 元素 | 说明 |
|------|------|
| 拍位格子 | 按拍号分列；开启细分时每拍再拆格 |
| 高亮 | 当前拍 / 细分格高亮；重拍用强调色 |
| Play / Pause | 圆角半透明灰色图标 + 阴影；点按启停 |

### 3.2 速度

- 范围：**40–240 BPM**
- 「速度」标签右侧小字显示当前值，如 `100 BPM`
- 滑条 + 左右 ±1 步进

### 3.3 拍号

- `2/4` · `3/4` · `4/4`（默认 4/4）

### 3.4 细分

| 选项 | 含义 |
|------|------|
| 四分 | 每拍 1 下（默认） |
| 八分 | 每拍 2 下 |
| 十六分 | 每拍 4 下 |

细分在界面上始终可视化；若开启「反拍静音」，反拍格子仍动，但不发声（用来练反拍默数）。

---

## 4. 级数页

### 4.1 顶部

| 元素 | 说明 |
|------|------|
| 标题 | `{调名} 大调`，如 `C 大调` |
| 唱名开关 | 开：显示 Do / Re / Mi…；关：环图显示级数数字 |

### 4.2 选调芯片

可选大调：

`C · G · D · A · E · B · F# · F · Bb · Eb · Ab · Db`

- 点芯片 → **切调**，并播放该调主音唱音
- 环图扇区 **只播唱音，不切调**

### 4.3 环图模式

- 七扇区，对应 C–B 自然音名环
- 当前调主音扇区高亮；轮盘旋转使主音在上方
- 每扇区：
  - 上行：**调内拼写音名**（如 F# 调里写 `E#`）
  - 下行：默认 **级数 1–7**；开唱名则为 `1 · Do` 形式
- 点扇区 → 只发出对应音高（唱音），**不切换大调**
- 中心显示当前调名

### 4.4 指板模式

| 项 | 说明 |
|----|------|
| 调弦 | 标准 **EADGBE**（图上自上而下：e B G D A E） |
| 品数 | **0–12**（0 = 开放弦） |
| 圆点 | 当前大调全部音阶音；**主音**用强调色 |
| 点上文字 | 音名；开唱名则显示唱名 |
| 点音 | 按真实品位 MIDI 发唱音；点主音还可切到该调 |
| 把位 | 见下表；窗外圆点淡化，窗内保持清晰 |

**把位（品区箱，非 CAGED）：**

| 标签 | 品区 |
|------|------|
| 全部 | 0–12 |
| 开放 | 0–4 |
| 3–7 | 3–7 |
| 5–9 | 5–9 |
| 7–11 | 7–11 |
| 9–12 | 9–12 |

### 4.5 视图切换

页底分段控件：**环图 | 指板**

- 互斥，同一时刻只显示一套主视觉
- 切换不丢失当前调 / 唱名状态

---

## 5. 设置页

### 5.1 声源（节拍器音色）

| 名称 | 风格 |
|------|------|
| 滴答 | 默认；正拍清晰、反拍更轻 |
| 鼓声 | 底鼓 / 军鼓感 |
| 木鱼 | 敲击感 |
| 拍手 | 掌击感 |

点选时会 **试听**；选择持久化到本地。

### 5.2 音量

- 总音量滑条（影响节拍器与唱音）
- 本地持久化

### 5.3 反拍静音

- 开关；开启后细分里的反拍 **有动画、无声音**
- 本地持久化

### 5.4 配色

| 名称 | 氛围 |
|------|------|
| 草地 | 黄绿 |
| 薄雾 | 灰白（默认） |
| 暮粉 | 粉杏 |
| 石墨 | 中性深灰氛围 |
| 焰橘 | 暖橘 |
| 潮汐 | 蓝青 |

选择会改页面氛围、主题色，并本地持久化。

---

## 6. 音频行为一览

| 操作 | 结果 |
|------|------|
| Play / Pause | 启停节拍器 |
| 点调芯片 | 切调 + 播主音唱音 |
| 点环图扇区 | 只播该音唱音 |
| 点指板圆点 | 按品位播唱音；主音可切调 |
| 选声源 | 试听该声源 |
| 反拍静音 | 反拍静音，正拍仍响 |

唱音为软合成音色（非人声采样），音高按标准音高（A4=440）。

---

## 7. 交互与布局原则

1. **手机一屏**：主内容区自适应填满，避免整页滚动条  
2. **一页一主视觉**：级数页环图与指板不叠两套  
3. **切调入口明确**：调芯片（+ 指板主音）；环图只听音  
4. **本地记忆**：声源、音量、反拍静音、配色  
5. **触控优先**：大点击区；避免误用的焦点框干扰

---

## 8. 本版不做

- CAGED 型名、指法数字  
- 非标准调弦、13 品以上  
- 扫弦动画、和弦进行练习  
- 账号 / 云同步  
- 人声采样唱名

---

## 9. 仓库结构与运行

```
scale-pulse/
├── APP.md
├── packages/core/   # Web 共享大调 / 指板 / pitch（TS）
├── webapp/          # Web（react-server + Vite Pages）
└── ios/             # iOS（SwiftUI，需 Mac + Xcode）
```

### Web

```bash
# 仓库根目录
npm run dev

# 或
cd webapp && npm install && npm run dev
```

### iOS

需 **macOS + Xcode**（Windows 无法编译）。见 [`ios/README.md`](./ios/README.md)。

```bash
cd ios && xcodegen generate && open ScalePulse.xcodeproj
```

节拍按**前台练习**设计，锁屏/后台不强制保活。

---

## 10. 快速验收清单

- [ ] 节拍：改 BPM / 拍号 / 细分，格子与声音一致  
- [ ] 反拍静音：反拍格仍闪、无声  
- [ ] 级数：切调后环图级数与指板圆点一致  
- [ ] 环图：点扇区只发声、不切调  
- [ ] 指板：把位窗外淡化；点音有声  
- [ ] 环图 ↔ 指板：调不丢  
- [ ] 设置：声源试听、音量、主题切换后刷新仍保留  
