# Scale Pulse

手机优先的 **节拍器 + 大调级数练习 + 吉他调音** 工具。一屏用完，无整页滚动。底部 Tab：**节拍 / 级数 / 调音 / 设置**。

在线预览（GitHub Pages）：<https://cy-98.github.io/SimpleGuitar-APP/>

---

## 1. 一句话定位

- **节拍**：练速度与拍感。  
- **级数**：在大调里认音、听音（环图 / 指板）。  
- **调音**：麦克风实时听音，标准或特殊调弦。  
- **设置**：声源、音量、反拍静音、主题；选项本地持久化。

节拍与级数、调音彼此独立；级数指板仍默认 **标准 EADGBE**（与调音 Tab 的特殊调弦无关）。

---

## 2. 信息架构

```
Scale Pulse
├── 节拍（默认页）
│   ├── 中央：拍位可视化 + Play / Pause
│   └── 控件：速度 / 拍号 / 细分
├── 级数
│   ├── 当前大调 + 「字母」开关（隐藏音名，仅留级数）
│   ├── 调芯片（选调）
│   ├── 主视觉：环图 或 指板
│   ├── 把位（仅指板）
│   └── 底部：环图 | 指板 切换
├── 调音
│   ├── 顶栏：标准 / 特殊 滑动开关 + 一行状态（Hz · 音名 · 音分）
│   ├── 六弦竖向「琴头朝上」：空弦对齐琴枕，音高向琴身（下）升高
│   ├── 自动麦克风；自动匹配最近弦；仅当前弦显示 live 圆点
│   └── 特殊模式：整弦拖动设目标；E=B 记谱；重置；本地记忆
└── 设置
    ├── 声源
    ├── 音量
    ├── 反拍静音
    └── 配色
```

---

## 3. 节拍页

### 3.1 中央舞台

| 元素 | 说明 |
|------|------|
| 拍位格子 | 按拍号分列；开启细分时每拍再拆格 |
| 高亮 | 当前拍 / 细分格高亮；重拍用强调色 |
| Play / Pause | 圆角半透明灰色图标 + 阴影；点按启停 |

### 3.2 速度

- 范围：**40–240 BPM**
- 「速度」标签右侧小字显示当前值，如 `100 BPM`
- 滑条 + 左右 ±1 步进

### 3.3 拍号

- `2/4` · `3/4` · `4/4`（默认 4/4）

### 3.4 细分

| 选项 | 含义 |
|------|------|
| 四分 | 每拍 1 下（默认） |
| 八分 | 每拍 2 下 |
| 十六分 | 每拍 4 下 |

细分在界面上始终可视化；若开启「反拍静音」，反拍格子仍动，但不发声（用来练反拍默数）。

---

## 4. 级数页

### 4.1 顶部

| 元素 | 说明 |
|------|------|
| 标题 | `{调名} 大调`，如 `C 大调` |
| 字母开关 | **开**：扇区 / 指板显示音名字母 + 级数；**关**：仅显示级数 1–7（不再使用唱名 do/re/mi） |

### 4.2 选调芯片

可选大调：

`C · G · D · A · E · B · F# · F · Bb · Eb · Ab · Db`

- 点芯片 → **切调**，并播放该调主音唱音
- 环图扇区 **只播唱音，不切调**

### 4.3 环图模式

- 七扇区，对应 C–B 自然音名环
- 当前调主音扇区高亮；轮盘旋转使主音在上方
- 每扇区：
  - 上行：**调内拼写音名**（如 F# 调里写 `E#`）
  - 下行：**级数 1–7**（关字母时可只显示级数）
- 点扇区 → 只发出对应音高（唱音），**不切换大调**
- 中心显示当前调名

### 4.4 指板模式

| 项 | 说明 |
|----|------|
| 调弦 | 标准 **EADGBE**（图上自上而下：e B G D A E） |
| 品数 | **0–12**（0 = 开放弦） |
| 圆点 | 当前大调全部音阶音；**主音**用强调色 |
| 点上文字 | 音名；关字母则显示级数 |
| 点音 | 按真实品位 MIDI 发唱音；点主音还可切到该调 |
| 把位 | 见下表；窗外圆点淡化，窗内保持清晰 |

**把位（品区箱，非 CAGED）：**

| 标签 | 品区 |
|------|------|
| 全部 | 0–12 |
| 开放 | 0–4 |
| 3–7 | 3–7 |
| 5–9 | 5–9 |
| 7–11 | 7–11 |
| 9–12 | 9–12 |

### 4.5 视图切换

页底分段控件：**环图 | 指板**

- 互斥，同一时刻只显示一套主视觉
- 切换不丢失当前调 / 字母开关状态

---

## 5. 调音页

设计原则：**界面尽量简单**——一屏六弦、一条读数、一个 live 点；不画品丝网格、不逐弦列音分。

### 5.1 进入与麦克风

- 进入 **调音** Tab 时 **自动请求并开启麦克风**；离开 Tab 时停止采集。
- 浏览器拒绝权限时，底部提示 +「重新授权」。
- 检测算法：**YIN 风格** 基频估计；A4 = 440 Hz。

### 5.2 顶栏

| 元素 | 说明 |
|------|------|
| 滑动开关 | **标准** / **特殊**（左上角） |
| 重置 | 仅 **特殊** 模式；恢复六弦出厂空弦 MIDI |
| 状态行 | 有信号：`频率 Hz · 音名八度 · ±音分`；无信号且为特殊模式：显示 **E=B** 记谱摘要（如 `E=D G=C`） |

音准在 **±5¢** 内时，舞台边框 / live 点使用「准音」强调样式。

### 5.3 六弦布局（琴头朝上）

- 列顺序（左 → 右）：**6 弦 … 1 弦**，琴头标签 **E A D G B e**（与 TAB 一致，1 弦为小写 e）。
- **琴枕（空弦目标）** 在上方同一高度；更高音高在视觉上向 **琴身方向（向下）** 移动（约 12 品行程）。
- **标准模式**：每弦琴枕处为 **♪** 按钮，点按播放该弦 **目标空弦** 参考音（合成唱音）。
- **特殊模式**：在 **整根弦轨道** 上拖动，设定该弦目标 MIDI；琴枕按钮显示 **`E=B` 式标签**（弦名 = 目标音名，必要时带八度）。
- **自动选弦**：实时 MIDI 与六弦目标比最近邻，高亮该弦列；**仅该弦** 显示 **live 圆点**（纵向位置 = 相对目标音高，横向微移 = 音分）。

### 5.4 特殊调弦持久化

- `localStorage` 键：`scale-pulse-custom-tuning`（每弦 MIDI）。
- 拖动范围：相对该弦 **出厂空弦** 约 **−6 ～ +12 半音**（与 UI 行程一致）。

### 5.5 与级数指板的关系

- 调音 Tab 的特殊调弦 **不改变** 级数页指板调弦显示（仍为标准 EADGBE）。
- 未来若做「变调夹 / 指板联动」，见路线图。

---

## 6. 设置页

### 6.1 声源（节拍器音色）

| 名称 | 风格 |
|------|------|
| 滴答 | 默认；正拍清晰、反拍更轻 |
| 鼓声 | 底鼓 / 军鼓感 |
| 木鱼 | 敲击感 |
| 拍手 | 掌击感 |

点选时会 **试听**；选择持久化到本地。

### 6.2 音量

- 总音量滑条（影响节拍器与唱音）
- 本地持久化

### 6.3 反拍静音

- 开关；开启后细分里的反拍 **有动画、无声音**
- 本地持久化

### 6.4 配色

| 名称 | 氛围 |
|------|------|
| 草地 | 黄绿 |
| 薄雾 | 灰白（默认） |
| 暮粉 | 粉杏 |
| 石墨 | 中性深灰氛围 |
| 焰橘 | 暖橘 |
| 潮汐 | 蓝青 |

选择会改页面氛围、主题色，并本地持久化。

---

## 7. 音频行为一览

| 操作 | 结果 |
|------|------|
| Play / Pause | 启停节拍器 |
| 点调芯片 | 切调 + 播主音唱音 |
| 点环图扇区 | 只播该音唱音 |
| 点指板圆点 | 按品位播唱音；主音可切调 |
| 进入调音 Tab | 开麦克风；实时读数 |
| 调音 · 标准 · ♪ | 播该弦目标参考音 |
| 调音 · 特殊 · 拖动 | 更新目标 MIDI 并持久化 |
| 选声源 | 试听该声源 |
| 反拍静音 | 反拍静音，正拍仍响 |

唱音为软合成音色（非人声采样），音高按标准音高（A4=440）。

---

## 8. 交互与布局原则

1. **手机一屏**：主内容区自适应填满，避免整页滚动条  
2. **一页一主视觉**：级数页环图与指板不叠两套  
3. **切调入口明确**：调芯片（+ 指板主音）；环图只听音  
4. **本地记忆**：声源、音量、反拍静音、配色、特殊调弦  
5. **触控优先**：大点击区；调音特殊模式支持整轨拖动  

---

## 9. 本版不做

- CAGED 型名、指法数字  
- 13 品以上指板  
- 和弦进行编辑器、扫弦动画  
- 账号 / 云同步  
- 人声采样唱名  
- 级数指板随特殊调弦自动改弦名（尚未做）

---

## 10. 路线图（计划文档）

详见 [`docs/plans/README.md`](./docs/plans/README.md)：

| 功能 | 说明 |
|------|------|
| [BPM 测速](./docs/plans/tap-tempo.md) | Tap 估 BPM，写入节拍器 |
| [变调夹](./docs/plans/capo.md) | 虚拟 capo，级数 / 指板 / 音高联动 |
| [节奏练习生成](./docs/plans/rhythm-practice.md) | 预设 pattern，驱动节拍引擎 |

建议实现顺序：**测速 → 变调夹 → 节奏练习**。

---

## 11. 近期更新（调音与级数）

以下为近期 Web 版主要迭代，便于对照代码与线上行为。

| 主题 | 内容 |
|------|------|
| 新 Tab | 底部增加 **调音**，与节拍 / 级数 / 设置并列 |
| 调音 · 自动化 | 进 Tab 自动开麦；**自动匹配最近弦**；无需手选弦 |
| 调音 · 几何 | 竖向六弦；**琴头朝上**；六弦 **空弦共线琴枕**；音高向下为升 |
| 调音 · 模式 | **标准 / 特殊** 滑动开关；特殊调弦 **E=B** 记谱 + 拖动设目标 + **重置** + localStorage |
| 调音 · 读数 | 顶栏单行：**Hz · 音名 · 音分**；准音高亮；UI **精简**（无品丝、无逐弦音分行、单 live 点） |
| 级数 | 「唱名」改为 **「字母」开关**：可隐藏音名只留级数 |
| 文档 | `docs/plans/`：变调夹、Tap BPM、节奏练习等后续方案 |

---

## 12. 仓库结构与运行

```
scale-pulse/
├── APP.md                 # 本文件（产品说明）
├── docs/plans/            # 后续功能计划
├── packages/core/         # Web 共享：大调 / 指板 / pitch / 音分（TS）
├── webapp/                # Web（react-server 开发 + Vite 构建 Pages）
└── ios/                   # iOS（SwiftUI，需 Mac + Xcode）
```

### Web 开发

```bash
# 仓库根目录
npm run dev

# 或
cd webapp && npm install && npm run dev
```

### GitHub Pages 构建

```bash
cd webapp && npm run build:pages
# 部署由 CI 完成，BASE_PATH=/SimpleGuitar-APP/
```

### iOS

需 **macOS + Xcode**（Windows 无法编译）。见 [`ios/README.md`](./ios/README.md)。

```bash
cd ios && xcodegen generate && open ScalePulse.xcodeproj
```

节拍按 **前台练习** 设计，锁屏/后台不强制保活。

---

## 13. 快速验收清单

- [ ] 节拍：改 BPM / 拍号 / 细分，格子与声音一致  
- [ ] 反拍静音：反拍格仍闪、无声  
- [ ] 级数：切调后环图级数与指板圆点一致  
- [ ] 级数：关「字母」后环图 / 指板仅显示级数  
- [ ] 环图：点扇区只发声、不切调  
- [ ] 指板：把位窗外淡化；点音有声  
- [ ] 环图 ↔ 指板：调与字母开关不丢  
- [ ] 调音：进 Tab 自动开麦；顶栏显示 Hz / 音名 / 音分  
- [ ] 调音：live 点只在当前匹配弦上移动；准音时有强调样式  
- [ ] 调音：标准模式 ♪ 参考音；特殊模式拖动 + E=B 标签 + 重置 + 刷新后仍保留  
- [ ] 设置：声源试听、音量、主题切换后刷新仍保留  
